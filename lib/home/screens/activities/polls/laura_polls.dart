import 'package:flutter/material.dart';

class LauraPolls extends StatelessWidget {
  const LauraPolls({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
